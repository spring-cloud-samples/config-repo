info:
  new: Demo for Ramnivas
  stuff: Hello World

---
spring:
  profiles: cloud
  platform:
    config:
      uri: ${vcap.services.${PREFIX:}configserver.credentials.uri:http://${PREFIX:}${configserver.${application.domain:cfapps.io}}}
application:
  domain: ${APPLICATION_DOMAIN:cfapps.io}
endpoints:
  restart: enabled
eureka:
  instance:
    hostname: ${PREFIX:}${spring.application.name}.${application.domain:cfapps.io}
    nonSecurePort: 80
  client:
    serviceUrl:
      defaultZone: ${vcap.services.${PREFIX:}eureka.credentials.uri:http://${PREFIX:}eureka.${application.domain:cfapps.io}}/v2/

---
spring:
  profiles: docker
  platform:
    config:
      uri: http://${CONFIGSERVER_1_PORT_8888_TCP_ADDR}:8888
endpoints:
  restart: enabled
eureka:
  instance:
    hostname: ${spring.application.name}_1
    nonSecurePort: 80
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_1_PORT_8761_TCP_ADDR:localhost}:8761/v2/

